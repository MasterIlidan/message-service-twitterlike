apiVersion: apps/v1
kind: Deployment
metadata:
  name: message-service-twitterlike
spec:
  replicas: 2
  selector:
    matchLabels:
      app: message-service-twitterlike
  template:
    metadata:
      labels:
        app: message-service-twitterlike
    spec:
      containers:
        - name: message-service-twitterlike
          image: localhost:32000/message-service-twitterlike:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8081